# PRD — 상품 판단을 위한 정보 인지 비용 개선

**프로젝트** [포폴] AI를 활용한 디자인 시스템 구축
**대상 서비스** 쿠팡(Coupang) — 부분 개선
**문서 버전** v0.1
**작성일** 2026-08-31
**상태** 디자인 착수 가능 (Foundation / Component 미정의)

---

## 0. 이 문서에 대하여

### 0.1 문서의 역할

이 PRD는 이후 단계의 **Single Source of Truth**다. 디자이너는 이 문서를 근거로 Foundation과 Component를 설계하고, AI Agent는 이 문서를 근거로 UI를 구현한다. 구현 결과가 의도와 다를 경우, 코드를 먼저 고치는 것이 아니라 **이 문서가 충분히 명확했는지를 먼저 검토한다.**

### 0.2 이 버전에서 정의하지 않는 것

현재는 Foundation과 Component 디자인 이전 단계다. 따라서 다음은 의도적으로 비워둔다.

- 구체적인 디자인 값 (색상 HEX, Spacing 수치, Radius, Font size, Elevation 등)
- 컴포넌트의 시각적 명세 (Anatomy의 픽셀 단위 정의, Variant별 스타일)
- 최종 화면 레이아웃

대신 **"어떤 조건을 만족해야 하는가"**를 요구사항으로 정의한다.

> 예시
> ❌ Button의 Radius는 8px이다
> ⭕ 사용자의 주요 행동은 한 화면에서 하나만 최상위로 인지되어야 한다

### 0.3 근거 수준 표기

이 문서에서 다루는 문제는 실제 사용자 리서치가 아니라 **디자이너 1인의 서비스 분석**에 기반한다. 따라서 서술 수준을 다음과 같이 구분해 표기한다.

| 표기 | 의미 |
|---|---|
| `[관찰]` | 서비스에서 실제로 확인한 사실 |
| `[판단]` | 관찰을 근거로 한 디자이너의 해석 |
| `[가설]` | 검증되지 않았으며, 이후 검증 대상 |
| `[가정]` | 리서치 없이 설정한 전제. 프로젝트 진행을 위한 임시 정의 |

---

## 1. 프로젝트 개요

### 1.1 배경

쿠팡은 다양한 할인과 혜택을 적극적으로 노출하는 커머스 서비스다. 이 프로젝트는 쿠팡을 사용하며 발견한 UX/UI 문제 중 일부를 선별해, 그 문제를 해결하는 과정에서 **디자인 원칙과 디자인 시스템을 직접 설계**하는 것을 목표로 한다.

쿠팡의 실제 디자인 시스템을 복제하거나, 쿠팡 전체를 리디자인하는 것이 목적이 아니다. 또한 이 프로젝트는 쿠팡의 현재 디자인이 잘못되었다고 전제하지 않는다. **높은 정보 밀도는 비즈니스 목표를 위한 전략적 선택일 가능성이 있으며,** 이 프로젝트는 그 선택의 반대편 —"정보 인지 비용을 낮췄을 때 어떤 시스템이 필요한가"— 를 설계 실험으로 다룬다.

### 1.2 이 프로젝트가 증명하려는 것

1. Foundation과 Component의 구조와 원리를 이해하고 직접 설계할 수 있다
2. AI Agent와 협업해 짧은 기간에 실제 UI 결과물을 만들 수 있다
3. 기획자·디자이너·개발자·AI Agent가 동일한 방향을 이해할 수 있도록 문서화할 수 있다

이 중 **3번이 이 프로젝트의 진짜 검증 대상**이다. 디자이너가 정의한 요구사항과 디자인 시스템을 AI Agent가 이해하고 UI에 일관되게 적용할 수 있는지가 성공 여부를 가른다.

### 1.3 범위

| 항목 | 정의 |
|---|---|
| 플랫폼 | 모바일 웹 (Mobile-first) |
| 대상 화면 | Product List (카테고리 목록 / 검색 결과), Product Detail |
| 제외 화면 | Home, 장바구니, 결제, 로그인, 마이페이지, 검색 입력 화면 |
| 제외 영역 | 개인화 추천 알고리즘 설계, 실제 백엔드/데이터 파이프라인, 성능 최적화 |
| 데이터 | 실제 쿠팡 데이터가 아닌, 구조가 동일한 샘플 데이터 사용 |

---

## 2. 문제 분석

### 2.1 관찰 정리

분석 과정에서 발견한 개별 문제는 다음과 같다.

**시각 표현 관련** `[관찰]`
- 아이콘에 다양한 색상이 사용됨
- 이미지 스타일이 통일되지 않은 경우가 있음
- 프로모션 / 광고 / 혜택 정보가 여러 영역에서 동시에 강조됨
- 전반적으로 시각적 정보량이 많음
- Product Detail 상단의 광고 배너가 상품 정보보다 먼저 노출됨
- 광고 영역이 주변 콘텐츠와 시각적으로 구분되지 않는 경우가 있음

**정보 구조 관련** `[관찰]`
- 가격 관련 정보가 하나의 묶음으로 인지되지 않는 경우가 있음
- 상품 특성 정보(배송 유형, 용량 등)의 위치가 카드마다 다름
- Product Detail에서 관련 정보가 여러 영역에 분산되어 있음
- 상품 이미지 위의 '상품 상세' 진입점과 실제 상세 정보 영역 사이에 거리가 있음
- 상품 목록에서 정보량이 많아 스크롤이 길어짐
- 동일한 정보가 반복적으로 노출되는 경우가 있음 (예: 로켓프레시 관련 정보)

**판단 근거 관련** `[관찰]`
- 리뷰 본문이 길어 핵심 내용 파악에 시간이 걸림
- 리뷰 수는 많지만 신뢰도를 판단할 단서가 부족함
- 전체 리뷰 수와 만족한 사람의 절대 숫자가 함께 노출됨
- '이 상품 놓치지 마세요!' 등 추천 영역의 추천 기준이 명시되지 않음

**탐색 상태 관련** `[관찰]`
- 배너 캐러셀에서 현재 위치를 파악하기 어려움
- 횡스크롤 영역의 전체 분량을 예측하기 어려움

### 2.2 문제 그룹화

개별 문제를 관통하는 축으로 재구성하면 5개 그룹이 도출된다.

| 그룹 | 관통하는 문제 |
|---|---|
| **G1. 강조의 위계가 시스템으로 관리되지 않음** | 강조 수단(색·크기·배지·배너)이 개별 판단으로 사용되어, 한 화면 안에 최상위 강조가 여러 개 존재 |
| **G2. 정보의 묶음과 위치가 화면마다 달라짐** | 같은 성격의 정보가 매번 다른 자리·다른 묶음으로 나타나 스캔 패턴을 학습할 수 없음 |
| **G3. 중복 노출과 밀도** | 같은 정보가 여러 컴포넌트에 반복 등장 |
| **G4. 판단 근거가 보이지 않음** | 무언가를 내세우지만(추천·리뷰·N명 만족) 왜/얼마나 믿을 수 있는지 알려주지 않음 |
| **G5. 현재 위치·전체 양 인지** | 캐러셀·횡스크롤에서 현재 위치와 잔여량을 알기 어려움 |

### 2.3 핵심 문제 선별

선정 기준에 따라 평가한 결과, **G1 / G2 / G4를 핵심 문제로 선정**한다.

| 기준 | G1 | G2 | G4 | G3 | G5 |
|---|---|---|---|---|---|
| ① 디자인 시스템과 연결되는가 | 강함 | 매우 강함 | 강함 | 약함 | 약함 |
| ② 취향이 아닌 문제로 설명 가능한가 | 강함 | 매우 강함 | 강함 | 취약 | 보통 |
| ③ 타 도메인 적용 가능한가 | 매우 강함 | 매우 강함 | 강함 | 보통 | 보통 |
| ④ AI가 구현 가능하게 정의 가능한가 | 보통 | 매우 강함 | 강함 | 보통 | 강함 |
| ⑤ 범위가 적절한가 | 적절 | 적절 | 적절 | — | 과소 |
| **결정** | **핵심 P1** | **핵심 P2** | **핵심 P3** | 흡수 | 흡수 |

**G3 (중복 노출과 밀도)** 는 독립 문제가 아니라 G1·G2의 증상으로 판단한다. `[판단]` 강조 대상이 통제되지 않고(G1) 정보의 소속이 정해지지 않으면(G2) 중복은 자연스러운 결과다. 따라서 별도 문제로 세우지 않고 P1·P2의 요구사항 안에서 다룬다. 중복 제거 자체를 목표로 삼으면 "덜어내는 것이 좋다"는 취향 논쟁이 되어 선정 기준 ②를 만족하지 못한다.

**G5 (탐색 상태 인지)** 는 도출되는 컴포넌트가 Carousel Indicator 수준으로 좁아 단독 핵심 문제로는 범위가 부족하다. 횡스크롤 영역이 Product List·Detail에 포함되는 한에서 P2의 하위 요구사항으로 다룬다.

### 2.4 핵심 문제 3개의 관계

세 문제는 사용자의 인지 순서를 따라 하나로 연결된다.

```
   무엇이 중요한가?          어디를 봐야 하는가?         왜 믿어야 하는가?
        P1          →              P2            →           P3
   강조의 위계             정보의 묶음과 위치           판단 근거의 노출
        ↓                         ↓                         ↓
   Color / Type            Spacing / Layout          Data 표기 / 신뢰 신호
   Badge / Ad              Card / Price Block        Rating / Review / Reason
```

이 흐름을 관통하는 상위 질문은 다음과 같다.

> **사용자가 구매를 판단하는 데 필요한 정보를, 얼마나 적은 인지 비용으로 얻을 수 있는가.**

---

## 3. Problem Definition

### P1. 강조의 위계가 시스템으로 관리되지 않는다

**관찰**
아이콘 색상, 프로모션 배지, 할인 표기, 광고 배너, 혜택 문구가 한 화면에서 동시에 강한 시각적 무게를 갖는다. 강조 수단이 서로 경쟁하며, 어떤 요소가 가장 중요한지 화면 자체가 말해주지 않는다. `[관찰]`

**왜 문제일 수 있는가 (Why)**
`[판단]` 시각적 강조는 유한한 자원이다. 강조된 요소의 수가 늘어날수록 개별 강조가 갖는 변별력은 낮아진다. 모든 것이 강조되면 사용자는 강조 자체를 신호로 받아들이지 않고, 결국 익숙한 위치를 기준으로 정보를 찾게 된다. 이 상태에서는 서비스가 정말로 알리고 싶은 정보(예: 실질 최저가, 오늘 도착)조차 다른 강조에 묻힐 수 있다.

또한 상업적 정보(광고·프로모션)와 제품 정보가 동일한 강조 수단을 공유하면, 사용자는 두 정보를 구분하는 데 추가 비용을 쓴다. `[판단]`

**관찰된 화면** Product List, Product Detail

**이 문제를 다루면 정의해야 하는 것**
강조는 몇 단계인가 / 각 단계는 어떤 시각 속성으로 표현되는가 / 한 화면·한 컴포넌트에서 각 단계는 몇 번 쓰일 수 있는가 / 상업적 정보는 어떤 표현 체계를 갖는가

---

### P2. 정보의 묶음과 위치가 화면마다 달라진다

**관찰**
가격 관련 정보(정가·할인율·판매가·단위가·쿠폰가)가 하나의 묶음으로 인지되지 않는 경우가 있다. 배송 유형·용량 같은 상품 특성 정보의 위치가 카드마다 다르다. Product Detail에서는 판단에 필요한 정보가 여러 영역에 분산되어 있고, 이미지 위의 '상품 상세' 진입점과 실제 상세 정보 영역 사이에 거리가 있다. `[관찰]`

**왜 문제일 수 있는가 (Why)**
`[판단]` 사용자는 목록에서 여러 상품을 **비교**한다. 비교는 같은 항목이 같은 자리에 있을 때만 병렬로 처리된다. 항목의 위치가 카드마다 달라지면 사용자는 비교 대신 카드마다 개별 읽기를 반복하게 되고, 이는 스크롤이 길어지는 원인이 된다. `[가설]`

또한 정보의 물리적 거리는 곧 관계의 신호다. 정가와 판매가가 떨어져 있으면 두 숫자가 같은 판단 단위라는 것을 사용자가 직접 조립해야 한다. Detail에서 진입점과 목적지가 멀면 사용자는 이동 결과를 예측하지 못하고, 예측 실패는 되돌아가기 행동을 만든다. `[판단]`

**관찰된 화면** Product List, Product Detail

**이 문제를 다루면 정의해야 하는 것**
각 화면은 어떤 정보 슬롯으로 구성되는가 / 어떤 정보가 같은 판단 단위인가 / 그룹 경계는 무엇으로 표현되는가 / 같은 정보 타입은 화면이 달라져도 동일한 상대 위치를 갖는가

---

### P3. 판단 근거가 드러나지 않는다

**관찰**
리뷰가 상세하지만 길어 핵심을 빠르게 파악하기 어렵다. 리뷰 수는 많지만 신뢰도를 판단할 단서가 부족하다. 전체 리뷰 수와 만족한 사람의 절대 숫자가 함께 노출된다. 추천 영역은 추천 이유를 밝히지 않는다. `[관찰]`

**왜 문제일 수 있는가 (Why)**
`[판단]` 절대 수치는 모수 없이는 해석되지 않는다. "7,000명 이상 만족"은 전체가 8,000명일 때와 70,000명일 때 전혀 다른 정보지만, 표현 형태가 같으면 사용자는 두 경우를 구분하지 못한다. 큰 숫자는 그 자체로 긍정 신호처럼 읽히기 때문에, 의도하지 않았더라도 사용자가 실제보다 높은 만족도로 오해할 여지가 있다. `[가설]`

리뷰 본문의 길이도 같은 구조의 문제다. 사용자가 리뷰에서 확인하려는 것은 대개 몇 가지로 수렴하지만(실제 사이즈, 사용 기간 후의 상태, 재구매 여부), 이 정보는 긴 본문 안에 섞여 있어 매번 읽어서 추출해야 한다. `[판단]`

추천 영역도 마찬가지다. 추천 이유가 없으면 그 영역은 사용자에게 "왜인지 모르지만 밀고 있는 상품"으로 인식되고, 광고와 구분되지 않는다. `[판단]`

**관찰된 화면** Product List(추천/노출 영역), Product Detail(리뷰, 사회적 증거)

**이 문제를 다루면 정의해야 하는 것**
수치는 어떤 형태로 표기되는가 / 신뢰 신호는 무엇이며 어떻게 표기되는가 / 리뷰의 핵심 정보는 무엇이고 어떤 순서로 노출되는가 / 근거가 부족할 때는 어떻게 표현하는가

---

## 4. Hypothesis

Problem Definition에서 도출된 가설이다. 이 프로젝트에서 정량 검증은 하지 않지만, **디자인 의사결정의 근거**이자 이후 사용자 테스트의 검증 대상으로 문서에 남긴다.

| ID | 가설 | 연결 문제 | 검증 방식 (이후 단계) |
|---|---|---|---|
| H1 | 강조 단계를 유한하게 정의하고 한 화면에서 최상위 강조의 사용 횟수를 제한하면, 사용자가 핵심 정보를 인지하는 속도가 개선될 수 있다 | P1 | 5초 테스트 — 화면 노출 후 기억나는 요소 회상 |
| H2 | 상업적 정보와 제품 정보가 구분되는 표현 체계를 가지면, 사용자가 두 정보를 분류하는 데 드는 비용이 줄어들 수 있다 | P1 | 화면 내 광고 영역 식별 정확도 |
| H3 | 같은 정보 타입이 항상 같은 슬롯에 위치하면, 사용자가 여러 상품을 병렬 비교할 수 있어 탐색 시간이 줄어들 수 있다 | P2 | 과제 수행 시간 — "배송이 가장 빠른 상품 찾기" |
| H4 | 판단 단위가 같은 정보를 하나의 그룹으로 묶으면, 가격 구조에 대한 오해가 줄어들 수 있다 | P2 | 이해도 질문 — "실제 결제 금액은 얼마인가" |
| H5 | 진입점과 목적지의 관계를 예측 가능하게 만들면, 되돌아가기 행동이 줄어들 수 있다 | P2 | 이동 후 즉시 뒤로가기 비율 |
| H6 | 절대 수치 대신 비율과 모수를 함께 제시하면, 사용자가 만족도를 더 정확하게 해석할 수 있다 | P3 | 해석 질문 — 동일 데이터 두 표현의 판단 차이 |
| H7 | 리뷰의 핵심 정보를 구조화해 먼저 노출하면, 사용자가 리뷰 영역에서 얻는 정보량이 늘어날 수 있다 | P3 | 리뷰 영역 체류 대비 획득 정보 수 |
| H8 | 추천 이유를 명시하면, 추천 영역이 광고와 구분되어 인지될 수 있다 | P3 | 추천 영역 신뢰도 응답 |

> 이 가설들은 **아직 검증되지 않았다.** PRD의 요구사항은 가설을 전제로 한 설계 방향이며, 검증 결과에 따라 수정될 수 있다.

---

## 5. Goal

### 5.1 Product Goal

사용자가 **상품을 판단하는 데 필요한 정보를, 더 적은 인지 비용으로 얻을 수 있게 한다.**

| # | 목표 | 연결 |
|---|---|---|
| PG-1 | 화면에서 가장 중요한 정보가 무엇인지 시각적으로 명확히 전달한다 | P1 |
| PG-2 | 상업적 정보와 제품 정보를 구분할 수 있게 한다 | P1 |
| PG-3 | 사용자가 여러 상품을 병렬로 비교할 수 있는 정보 구조를 만든다 | P2 |
| PG-4 | 사용자가 화면 내 이동 결과를 예측할 수 있게 한다 | P2 |
| PG-5 | 사용자가 리뷰와 추천의 근거를 빠르게 확인할 수 있게 한다 | P3 |
| PG-6 | 수치 정보를 오해 없이 해석할 수 있게 표기한다 | P3 |

### 5.2 Design System Goal

| # | 목표 |
|---|---|
| DG-1 | 강조 위계를 Foundation 레벨에서 정의하고, 모든 컴포넌트가 그 위계를 따르게 한다 |
| DG-2 | 화면의 모든 시각 속성이 토큰으로 설명 가능하게 한다 (하드코딩된 값 없음) |
| DG-3 | Product List와 Product Detail이 공통 컴포넌트를 최대한 공유하게 한다 |
| DG-4 | 컴포넌트의 사용 조건(언제 쓰고 언제 쓰지 않는가)을 문서로 정의한다 |
| DG-5 | 커머스 고유 개념(가격·배송·리뷰)에 종속되지 않도록 Foundation과 범용 Component를 도메인 중립적으로 설계한다 |

> DG-5는 이후 타 도메인 적용 검증을 위한 것이다. 검증 대상 도메인은 현 시점에 확정하지 않는다. 대신 **"Foundation과 범용 Component의 이름·구조에 커머스 용어를 쓰지 않는다"**는 규칙을 지금부터 적용한다. (예: `price-red` ❌ → `color-emphasis-critical` ⭕)

### 5.3 Documentation Goal

| # | 목표 |
|---|---|
| MG-1 | AI Agent가 추가 질문 없이 구현할 수 있을 만큼 요구사항이 구체적이다 |
| MG-2 | 구현 결과가 의도와 다를 때, 원인이 문서인지 구현인지 판별할 수 있다 |
| MG-3 | 디자인 의도(왜 이렇게 만들었는가)가 규칙과 함께 기록되어 있다 |

### 5.4 Non-Goal

- 쿠팡 전체 서비스 리디자인
- 쿠팡의 실제 디자인 시스템 복제 또는 추정
- 구매 전환율 등 비즈니스 지표 개선의 증명
- 추천 알고리즘 설계
- 결제·배송·계정 등 커머스 전체 플로우 커버
- 실제 서비스 배포

---

## 6. Target User

> `[가정]` 이 프로젝트는 실제 사용자 리서치를 수행하지 않았다. 아래 사용자 정의는 문제 해결의 기준점을 잡기 위한 전제이며, 관찰된 문제로부터 역으로 도출한 것이다. 실제 사용자 데이터로 검증되지 않았음을 명시한다.

### 6.1 Primary User

**"필요한 물건을, 크게 실패하지 않고, 빨리 사고 싶은 사람"**

| 항목 | 정의 |
|---|---|
| 구매 목적 | 탐색적 쇼핑보다는 목적형 구매가 많음 |
| 사용 맥락 | 모바일, 이동 중이거나 짧은 틈새 시간. 한 손 조작 |
| 의사결정 방식 | 여러 상품을 비교하되, 깊게 조사하지 않음. 몇 개의 판단 축(가격·배송·평가)에서 빠르게 결론을 내림 |
| 리뷰 사용 방식 | 리뷰를 전부 읽지 않음. 특정 정보(실제 사용감, 사이즈, 재구매 여부)만 확인하려 함 |
| 서비스 숙련도 | 쿠팡을 반복 사용해왔고, 화면 구조에 어느 정도 익숙함 |

### 6.2 이 사용자에게 중요한 것

1. **비교 가능성** — 여러 후보 중 무엇이 더 나은지 나란히 판단할 수 있어야 한다
2. **예측 가능성** — 탭했을 때 어디로 가는지, 무엇을 보게 될지 알 수 있어야 한다
3. **해석 가능성** — 화면에 있는 숫자와 표기를 오해 없이 읽을 수 있어야 한다

### 6.3 이번 범위에서 다루지 않는 사용자

- 첫 방문 사용자 (온보딩·서비스 이해 관련 문제)
- 탐색형 쇼핑 사용자 (발견·영감 중심 경험)
- 대량 구매 / B2B 사용자

---

## 7. User Scenario

### Scenario 1 — 목록에서 후보 좁히기

**상황** 사용자는 카테고리를 통해 상품 목록에 진입했다. 10개 내외의 상품을 훑으며 후보를 2~3개로 좁히려 한다.

**As-is 관찰** `[관찰]`
- 카드마다 강조되는 요소가 달라, 어떤 카드가 더 나은 조건인지 즉각 비교되지 않는다
- 가격 관련 숫자가 여러 개 노출되지만 어떤 것이 실제 지불 금액인지 한 번에 읽히지 않는다
- 배송 정보나 용량 같은 특성 정보가 카드마다 다른 위치에 있어 매 카드를 다시 읽는다
- 광고 상품과 일반 상품이 시각적으로 유사하다

**To-be 기대 흐름**
1. 사용자는 목록에 진입해 카드들을 위에서 아래로 훑는다
2. 모든 카드에서 같은 자리에 같은 종류의 정보가 있어, 사용자는 **한 축씩** 비교한다 (가격 열, 배송 열, 평가 열)
3. 각 카드에서 가장 강조된 요소는 하나뿐이며, 그것은 그 상품의 핵심 판단 근거다
4. 광고 상품은 표기로 구분되어, 사용자는 그것을 다른 기준으로 판단한다
5. 사용자는 2~3개의 후보를 정하고 하나를 탭한다

**충족되어야 하는 것** PG-1, PG-2, PG-3

---

### Scenario 2 — 상세에서 판단 완결하기

**상황** 사용자는 목록에서 고른 상품의 상세 화면에 진입했다. 이 상품을 살지 말지 결정하려 한다.

**As-is 관찰** `[관찰]`
- 상단에 광고 배너가 있어 상품 정보 확인이 지연된다
- 판단에 필요한 정보(가격 구성, 옵션, 배송, 상품 스펙, 리뷰)가 여러 영역에 분산되어 있다
- 이미지 위 '상품 상세' 진입점을 탭했을 때 도달하는 위치가 예측되지 않는다
- 목록에서 봤던 정보가 상세에서 다른 형태로 다시 나타난다

**To-be 기대 흐름**
1. 사용자는 상세에 진입해 먼저 상품이 무엇인지 확인한다 (이미지, 상품명, 핵심 스펙)
2. 이어서 조건을 확인한다 (가격 구성, 옵션, 배송)
3. 가격 블록은 목록에서 본 것과 **같은 구성 규칙**을 따르므로, 사용자는 목록의 정보와 상세의 정보를 연결해 이해한다
4. 다음으로 판단 근거를 확인한다 (평가 요약, 리뷰)
5. 화면 내 어느 정보 그룹으로도 이동할 수 있고, 이동 결과가 예측된다
6. 광고 영역은 하나의 그룹으로 묶여 있어, 사용자는 그 구간을 건너뛸지 스스로 결정한다
7. 사용자는 구매 행동을 취한다

**충족되어야 하는 것** PG-1, PG-2, PG-3, PG-4

---

### Scenario 3 — 리뷰로 확신 얻기

**상황** 사용자는 상품이 마음에 들지만 확신이 부족하다. 다른 사람들의 평가를 확인하려 한다.

**As-is 관찰** `[관찰]`
- 만족한 사람의 절대 숫자가 강조되지만 모수를 함께 인지하기 어렵다
- 리뷰 본문이 길어 원하는 정보를 찾기 위해 여러 개를 읽어야 한다
- 이 리뷰를 얼마나 믿어도 되는지 판단할 단서가 부족하다

**To-be 기대 흐름**
1. 사용자는 리뷰 영역에 도달해 먼저 **요약**을 본다
2. 요약은 비율과 모수를 함께 제시하므로, 사용자는 이 평가가 얼마나 대표성이 있는지 함께 판단한다
3. 개별 리뷰는 핵심 정보(사용 기간, 재구매 여부, 선택한 옵션)를 먼저 보여주고 본문은 접혀 있다
4. 사용자는 자신의 관심사와 맞는 리뷰만 펼쳐 읽는다
5. 표본이 적은 상품에서는 그 사실이 표시되어, 사용자가 높은 비율을 과신하지 않는다
6. 사용자는 구매 여부를 결정한다

**충족되어야 하는 것** PG-5, PG-6

---

## 8. User Flow

### 8.1 전체 플로우

```
        [진입: 카테고리 / 검색 결과]
                    │
                    ▼
        ┌─────────────────────────┐
        │     Product List        │
        │  · 카드 훑기             │
        │  · 축 단위 비교          │
        │  · 광고 상품 식별        │
        └─────────────────────────┘
                    │  상품 선택
                    ▼
        ┌─────────────────────────┐
        │    Product Detail       │
        │                         │
        │  ① 무엇인가              │  이미지 · 상품명 · 핵심 스펙
        │        ↓                │
        │  ② 조건은 어떤가         │  가격 블록 · 옵션 · 배송
        │        ↓                │
        │  ③ 믿을 만한가           │  평가 요약 · 리뷰
        │        ↓                │
        │  ④ 상세 정보             │  상품 상세 설명
        │        ↓                │
        │  ⑤ 참고 정보 (광고 그룹)  │  연관/추천/광고
        └─────────────────────────┘
                    │
          ┌─────────┼─────────┐
          ▼         ▼         ▼
       [구매]   [목록 복귀]  [리뷰 더보기]
```

### 8.2 Product Detail 내 이동 플로우

```
사용자가 특정 정보를 찾으려 함
        │
        ├── 화면 내 정보 그룹 이동 장치 사용
        │        │
        │        ▼
        │   해당 그룹으로 이동
        │        │
        │        ▼
        │   현재 위치가 어느 그룹인지 항상 확인 가능
        │
        └── 스크롤로 순차 탐색
                 │
                 ▼
            그룹 경계가 인지 가능
```

**핵심 규칙** 진입점과 목적지는 직접 연결된다. `[관찰]`에서 확인된 "이미지 위 상세 버튼 → 실제 상세 정보까지의 거리" 문제는, 진입점이 정보 그룹 이동 장치와 동일한 체계에 속하도록 하여 해결한다.

### 8.3 리뷰 확인 플로우

```
   리뷰 그룹 도달
        │
        ▼
   평가 요약 (비율 + 모수 + 신뢰 상태)
        │
        ├── 요약만으로 판단 충분 → 종료
        │
        └── 더 알아보고 싶음
                 │
                 ▼
            리뷰 목록
                 │
                 ├── 각 리뷰: 핵심 정보 노출 / 본문 접힘
                 │
                 ├── 관심 리뷰 펼치기
                 │
                 └── 조건으로 좁히기 (사용 기간 / 재구매 / 옵션)
```

---

## 9. Functional Requirements

우선순위: **P0** 핵심 문제 해결에 필수 / **P1** 있으면 좋음 / **P2** 여유 시

### 9.1 공통 (Product List + Product Detail)

| ID | 요구사항 | 연결 | 우선순위 |
|---|---|---|---|
| FR-C1 | 화면 내 강조는 유한한 단계로 정의되며, 모든 요소는 그중 하나의 단계에 속한다 | P1 | P0 |
| FR-C2 | 하나의 화면(또는 카드 단위)에서 최상위 강조는 **하나만** 사용한다 | P1 | P0 |
| FR-C3 | 광고 및 스폰서 콘텐츠는 일반 콘텐츠와 구분 가능한 표기를 가진다 | P1 | P0 |
| FR-C4 | 가격 관련 정보(정가·할인율·판매가·쿠폰 적용가·단위가)는 하나의 그룹으로 묶여 노출된다 | P2 | P0 |
| FR-C5 | 가격 그룹은 Product List와 Product Detail에서 **동일한 구성 규칙**을 따른다 | P2 | P0 |
| FR-C6 | 상품 특성 정보(배송 유형·용량/수량·옵션 유무)는 모든 화면에서 정해진 슬롯 순서를 따른다 | P2 | P0 |
| FR-C7 | 동일한 정보는 한 화면에서 최상위 강조로 한 번만 노출한다. 반복이 필요한 경우 하위 강조 단계로 표현한다 | P1, P2 | P0 |
| FR-C8 | 아이콘은 정보 전달 목적일 때만 사용하며, 색상은 의미가 있을 때만 부여한다 | P1 | P0 |
| FR-C9 | 상품 이미지는 공통 규격(비율·배경·여백 처리)을 따른다 | P1 | P1 |
| FR-C10 | 수치 정보는 정해진 표기 규칙을 따른다 (단위, 자릿수, 반올림, 모수 병기 여부) | P3 | P0 |

### 9.2 Product List

| ID | 요구사항 | 연결 | 우선순위 |
|---|---|---|---|
| FR-L1 | 상품 카드는 고정된 정보 슬롯 세트로 구성되며, 모든 카드가 동일한 슬롯 순서를 따른다 | P2 | P0 |
| FR-L2 | 정보가 없는 슬롯은 비워두거나 축소하되, 다른 슬롯의 위치를 밀어내지 않는다 | P2 | P0 |
| FR-L3 | 카드 내에서 강조되는 요소는 하나이며, 그 대상은 규칙으로 정의된다 (예: 해당 상품의 가장 유리한 조건 1개) | P1 | P0 |
| FR-L4 | 프로모션/혜택 표기는 카드당 노출 상한을 가지며, 상한 초과 시 처리 규칙을 따른다 | P1 | P0 |
| FR-L5 | 목록은 정보 밀도가 다른 카드 변형을 제공한다 (이미지 중심 / 정보 중심) | P2 | P1 |
| FR-L6 | 광고 상품은 표기와 함께, 연속 노출 시 하나의 그룹으로 인지되도록 처리한다 | P1 | P1 |
| FR-L7 | 평가 정보는 별점 수치 단독이 아니라 모수와 함께 노출한다 | P3 | P0 |
| FR-L8 | 추천/큐레이션 영역은 추천 근거를 짧게 명시한다 | P3 | P1 |
| FR-L9 | 횡스크롤 영역은 현재 위치와 전체 분량을 인지할 수 있는 수단을 갖는다 | P2 (G5) | P2 |

### 9.3 Product Detail

| ID | 요구사항 | 연결 | 우선순위 |
|---|---|---|---|
| FR-D1 | 화면은 판단 순서에 따른 정보 그룹으로 구획된다 (무엇인가 → 조건 → 근거 → 상세 → 참고) | P2 | P0 |
| FR-D2 | 각 정보 그룹은 경계가 인지 가능하며, 그룹의 성격을 알 수 있는 표시를 갖는다 | P2 | P0 |
| FR-D3 | 화면 내 정보 그룹으로 직접 이동할 수 있는 장치를 제공하며, 현재 위치를 항상 표시한다 | P2 | P0 |
| FR-D4 | 이미지 영역의 상세 정보 진입점은 FR-D3의 이동 장치와 동일한 체계에 속하며, 목적지가 예측 가능하다 | P2 | P0 |
| FR-D5 | 광고 및 추천 영역은 하나의 그룹으로 묶이며, 판단 정보 흐름(그룹 ①~③)을 분절하지 않는 위치에 배치한다 | P1 | P0 |
| FR-D6 | 구매 행동(CTA)은 화면 어디에서나 접근 가능하며, 항상 최상위 강조 단계를 점유한다 | P1 | P0 |
| FR-D7 | 옵션 선택은 가격 그룹과 연결되어, 선택에 따른 최종 금액 변화를 즉시 확인할 수 있다 | P2 | P1 |
| FR-D8 | 평가 요약은 만족도 **비율**과 **모수**를 함께 제시한다. 절대 수치 단독 노출을 금지한다 | P3 | P0 |
| FR-D9 | 평가 요약은 표본 규모에 따른 신뢰 상태를 함께 표현한다 | P3 | P0 |
| FR-D10 | 개별 리뷰는 핵심 정보(사용 기간·재구매 여부·선택 옵션·평점)를 본문보다 먼저 노출한다 | P3 | P0 |
| FR-D11 | 리뷰 본문은 접힌 상태로 시작하며, 사용자가 펼쳐 읽는다 | P3 | P0 |
| FR-D12 | 리뷰는 신뢰 신호를 기준으로 좁혀볼 수 있다 (사용 기간, 재구매, 옵션) | P3 | P1 |
| FR-D13 | 상세 설명 영역의 이미지·텍스트는 앞선 정보 그룹과 중복되는 내용을 최상위 강조로 반복하지 않는다 | P1, P2 | P1 |

---

## 10. UI Requirements

> 이 장은 **디자인이 만족해야 하는 조건**을 정의한다. 구체적인 값은 Foundation / Component 설계 이후 v0.2에서 추가한다.

### 10.1 강조 위계 (P1)

| ID | 요구사항 |
|---|---|
| UR-E1 | 강조는 **3단계 이하**로 정의한다. 각 단계는 "무엇을 위해 존재하는가"가 문서로 정의되어야 한다 |
| UR-E2 | 각 강조 단계는 사용할 수 있는 시각 속성 조합(색·크기·굵기·면적·배경)이 규정되어야 한다. 같은 단계를 서로 다른 방식으로 표현하지 않는다 |
| UR-E3 | 최상위 강조는 화면·카드 단위로 **1회**만 사용한다. 규칙 위반은 구현 검증 시 결함으로 취급한다 |
| UR-E4 | 색상은 강조 수단이 아니라 **의미**를 갖는다. 각 색은 어떤 의미에 대응하는지 정의되어야 하며, 의미 없는 장식 색상은 사용하지 않는다 |
| UR-E5 | 상업적 정보(광고·프로모션)는 제품 정보와 **다른 표현 체계**를 갖는다. 두 체계가 같은 시각 속성을 공유하지 않아야 한다 |
| UR-E6 | 아이콘은 단일 색 체계를 기본으로 하며, 다색 아이콘은 별도의 사용 조건이 정의된 경우에만 허용한다 |
| UR-E7 | 상품 이미지는 비율·배경·크롭 방식이 통일된 규격을 따른다. 규격을 벗어나는 이미지의 처리 방식이 정의되어야 한다 |

### 10.2 정보 그룹핑과 배치 (P2)

| ID | 요구사항 |
|---|---|
| UR-G1 | Spacing 체계는 **그룹 경계를 표현할 수 있어야 한다.** 그룹 내부 간격과 그룹 사이 간격이 명확히 구분되는 단계를 가져야 한다 |
| UR-G2 | 같은 판단 단위의 정보는 다른 그룹의 정보보다 시각적으로 더 가깝게 배치된다 |
| UR-G3 | 각 화면은 **정보 슬롯 구조(Page Anatomy)** 가 문서로 정의되어야 한다. 어떤 정보가 어느 슬롯에 오는지, 슬롯의 순서가 무엇인지 명시한다 |
| UR-G4 | 동일한 정보 타입은 Product List와 Product Detail에서 **동일한 상대적 위치 관계**를 갖는다 |
| UR-G5 | 텍스트 축약(말줄임) 규칙이 정의되어야 한다. 어떤 정보가 축약 가능하고 어떤 정보는 축약 불가인지 명시한다 |
| UR-G6 | 정보 그룹은 헤더 또는 구분 요소로 성격이 인지되어야 한다 |
| UR-G7 | 화면 내 이동 장치는 현재 위치를 표시해야 한다 |
| UR-G8 | 모바일 웹 기준 한 손 조작을 고려하여, 반복적으로 사용되는 조작 요소의 도달 범위를 정의한다 |
| UR-G9 | 터치 대상의 최소 크기 기준을 정의하고 모든 인터랙티브 요소가 이를 만족한다 |

### 10.3 판단 근거의 표현 (P3)

| ID | 요구사항 |
|---|---|
| UR-D1 | 비율을 표현하는 시각 형식이 정의되어야 한다. 비율은 **모수와 항상 함께** 제시된다 |
| UR-D2 | 절대 수치를 단독으로 강조하는 표현은 사용하지 않는다 |
| UR-D3 | 신뢰 신호(재구매·사용 기간·구매 확인)는 일관된 표기 형식을 갖는다 |
| UR-D4 | 표본이 적은 경우를 표현하는 **저신뢰 상태**의 시각 표현이 정의되어야 한다 |
| UR-D5 | 추천 근거를 표시하는 표현 형식이 정의되어야 한다. 이 형식은 광고 표기와 시각적으로 구분되어야 한다 |
| UR-D6 | 리뷰 카드는 핵심 정보 영역과 본문 영역이 구조적으로 구분되어야 한다 |
| UR-D7 | 접힘/펼침 상태가 명확히 인지되어야 하며, 펼침 시 화면 위치가 급격히 변하지 않아야 한다 |
| UR-D8 | 데이터가 없거나 부족한 상태(리뷰 0건 등)의 표현이 정의되어야 한다 |

### 10.4 시스템 전반

| ID | 요구사항 |
|---|---|
| UR-S1 | 모든 시각 속성은 토큰으로 정의되며, 구현에서 하드코딩된 값을 사용하지 않는다 |
| UR-S2 | Foundation과 범용 Component의 명칭은 커머스 도메인 용어를 포함하지 않는다 |
| UR-S3 | 각 컴포넌트는 **사용 조건과 사용 금지 조건**이 함께 문서화되어야 한다 |
| UR-S4 | 컴포넌트의 상태(기본·선택·비활성·로딩·오류)가 정의되어야 한다 |
| UR-S5 | 색상 대비는 접근성 기준을 만족해야 한다 (기준 수준은 Open Question 참조) |

---

## 11. Design System Requirements (v0.2에서 채울 영역)

현재 단계에서는 **도출이 예상되는 항목만** 기록한다. 실제 값과 명세는 Figma 설계 이후 추가한다.

### 11.1 Foundation (예상)

| 영역 | 이 프로젝트에서 정의해야 하는 이유 | 상태 |
|---|---|---|
| Color | 강조 위계 표현, 의미 기반 색 역할, 상업 정보 구분 체계 (UR-E4, E5) | 미정의 |
| Typography | 강조 단계의 타이포 표현, 정보 위계 (UR-E2) | 미정의 |
| Spacing | 그룹 경계 표현 (UR-G1, G2) | 미정의 |
| Layout / Grid | 카드 슬롯 구조, 화면 구획 (UR-G3) | 미정의 |
| Radius | 그룹 경계 보조 표현 | 미정의 |
| Elevation | 강조 위계 보조 표현, 고정 영역 처리 | 미정의 |
| Icon | 사용 조건과 색 규칙 (UR-E6) | 미정의 |
| Image | 규격과 예외 처리 (UR-E7) | 미정의 |
| Motion | 접힘/펼침, 위치 이동 (UR-D7) | 미정의 |

### 11.2 Component (예상)

**범용 (도메인 중립)**
Badge / Label / Tag · Button · Chip · Divider · Accordion · Tab · Anchor Navigation · Carousel + Indicator · Rating Display · Ratio Visualization · Empty State · Skeleton

**도메인 특화 (커머스)**
Product Card (변형 다수) · Price Block · Spec Row · Promotion Badge · Ad Container · Review Summary · Review Card · Evidence Chip · Reason Label

### 11.3 Pattern (예상)

| 패턴 | 목적 |
|---|---|
| Emphasis Hierarchy | 한 화면에서 강조를 어떻게 배분하는가 |
| Commercial Content Disclosure | 광고·프로모션을 어떻게 구분해 표기하는가 |
| Page Anatomy | 화면별 정보 슬롯 구조 |
| Information Grouping | 무엇을 같은 그룹으로 묶는가 |
| Progressive Disclosure | 무엇을 먼저 보여주고 무엇을 접는가 |
| Evidence Disclosure | 근거와 신뢰도를 어떻게 표현하는가 |

### 11.4 v0.2 업데이트 체크리스트

- [ ] Foundation 토큰 값 및 명명 규칙
- [ ] 강조 3단계의 구체적 시각 정의
- [ ] 컴포넌트별 Anatomy / Variant / State
- [ ] 컴포넌트별 사용 조건 및 금지 조건
- [ ] Page Anatomy (List / Detail) 확정
- [ ] 반응형 동작 규칙
- [ ] 접근성 기준 확정
- [ ] AI Agent용 구현 규칙 요약 (위반 시 결함이 되는 규칙 목록)

---

## 12. Edge Cases

### 12.1 데이터 결손·과잉

| # | 상황 | 정의해야 하는 처리 |
|---|---|---|
| E-1 | 프로모션·혜택이 하나도 없는 상품 | 해당 슬롯 처리 방식. 다른 슬롯 위치가 밀리지 않아야 함 (FR-L2) |
| E-2 | 프로모션·혜택이 상한을 초과하는 상품 | 노출 우선순위 규칙과 나머지 처리 (FR-L4) |
| E-3 | 정가·할인 정보가 없어 가격이 단일 값인 상품 | 가격 그룹의 최소 구성 |
| E-4 | 쿠폰·적립 등 조건부 가격이 있는 상품 | 조건부 금액의 표기 방식과 강조 단계 |
| E-5 | 상품명이 매우 긴 경우 / 매우 짧은 경우 | 축약 규칙과 최소 표시 줄 수 (UR-G5) |
| E-6 | 상품 이미지가 없거나 규격에 맞지 않는 경우 | 대체 표현 (UR-E7) |
| E-7 | 옵션이 매우 많은 상품 | 옵션 선택 UI의 확장 처리 |

### 12.2 신뢰·근거 관련

| # | 상황 | 정의해야 하는 처리 |
|---|---|---|
| E-8 | 리뷰 0건 | 평가 요약 영역의 빈 상태 (UR-D8) |
| E-9 | 리뷰가 소수 (예: 1~9건) | 저신뢰 상태 표현. 비율 표기의 적절성 판단 (FR-D9, UR-D4) |
| E-10 | 만족도 비율은 높으나 모수가 극히 적음 (예: 100% / 3명) | **과신 방지 표현 필수.** 이 케이스는 P3의 핵심 검증 대상 |
| E-11 | 리뷰에 본문 없이 이미지·평점만 있는 경우 | 리뷰 카드의 최소 구성 |
| E-12 | 재구매 여부·사용 기간 데이터가 없는 리뷰 | 신뢰 신호 부재 시 표기 (누락 vs 미표기 구분) |
| E-13 | 추천 근거를 산출할 수 없는 추천 영역 | 근거 없는 추천의 노출 여부 결정 (FR-L8) |

### 12.3 상태·맥락

| # | 상황 | 정의해야 하는 처리 |
|---|---|---|
| E-14 | 품절 / 판매 중지 상품 | 카드 및 상세의 상태 표현. CTA 대체 (FR-D6) |
| E-15 | 목록 상단에 광고 상품이 연속 노출 | 그룹 인지 처리 (FR-L6) |
| E-16 | 검색 결과 0건 | 빈 상태 |
| E-17 | 네트워크 지연으로 이미지가 늦게 로드 | 로딩 상태. 로드 후 레이아웃 이동(Shift) 방지 |
| E-18 | 긴 상세 설명으로 화면이 매우 길어지는 경우 | 이동 장치의 지속적 접근성 (FR-D3) |
| E-19 | 사용자가 접힘/펼침을 반복하는 경우 | 스크롤 위치 유지 (UR-D7) |

---

## 13. Success Criteria

이 프로젝트의 성공은 **세 층위**에서 판단한다. 세 번째가 이 프로젝트의 고유한 목표다.

### 13.1 제품 관점 — 문제가 해결되었는가

> 실사용 데이터가 없으므로 **비교 평가**로 판단한다. 개선안과 현재 화면을 나란히 두고 검증한다.

| # | 기준 | 방법 | 연결 |
|---|---|---|---|
| SC-P1 | 화면 노출 직후 인지되는 강조 요소가 소수로 수렴한다 | 5초 테스트 — 회상 항목 수집 | H1 |
| SC-P2 | 광고 영역을 정확히 식별할 수 있다 | 화면에서 광고 영역 지목 과제 | H2 |
| SC-P3 | 조건이 다른 여러 상품 중 특정 조건을 만족하는 상품을 더 빨리 찾는다 | 과제 수행 시간 비교 | H3 |
| SC-P4 | 실제 결제 금액을 오해 없이 답할 수 있다 | 이해도 질문 | H4 |
| SC-P5 | 만족도 정보를 모수와 함께 해석한다 (E-10 케이스에서 과신하지 않는다) | 동일 데이터 두 표현 비교 질문 | H6 |
| SC-P6 | 리뷰 영역에서 원하는 정보를 더 적은 탐색으로 얻는다 | 과제 수행 관찰 | H7 |

### 13.2 디자인 시스템 관점 — 시스템으로 성립하는가

| # | 기준 | 판정 방법 |
|---|---|---|
| SC-D1 | 구현된 화면의 모든 시각 속성이 토큰으로 설명된다 | 코드 내 하드코딩 값 검출 = 0 |
| SC-D2 | Product List와 Product Detail이 공통 컴포넌트를 공유한다 | 두 화면에서 공유되는 컴포넌트 비율 측정 |
| SC-D3 | 새로운 화면 요구가 발생했을 때 정의된 Variant 안에서 해결된다 | 신규 예외 컴포넌트 발생 건수 |
| SC-D4 | 모든 컴포넌트에 사용 조건과 금지 조건이 문서화되어 있다 | 문서 커버리지 100% |
| SC-D5 | Foundation과 범용 Component 명칭에 커머스 용어가 없다 | 명명 검수 |
| SC-D6 | 강조 위계 규칙(UR-E3)이 모든 화면에서 위반 없이 지켜진다 | 화면별 최상위 강조 개수 점검 |

### 13.3 문서·AI 협업 관점 — 이 PRD가 제 역할을 했는가

> **이 프로젝트의 핵심 검증 항목이다.**

| # | 기준 | 판정 방법 |
|---|---|---|
| SC-M1 | AI Agent가 PRD와 디자인 시스템 문서만으로 구현을 완료한다 | 구현 중 추가 질문·임의 결정 발생 건수 |
| SC-M2 | 구현 결과가 요구사항을 위반하지 않는다 | FR·UR 항목별 준수 여부 체크리스트 |
| SC-M3 | 위반이 발생했을 때 원인을 **문서 불명확 / AI 오류**로 분류할 수 있다 | 위반 건별 원인 분류 기록 |
| SC-M4 | 문서 불명확이 원인인 경우, PRD 수정 후 재구현에서 해결된다 | 재구현 성공률 |
| SC-M5 | 디자인 의도(왜 이렇게 만들었는가)가 규칙과 함께 기록되어 있다 | 규칙별 근거 서술 커버리지 |

> **SC-M3의 기록이 이 프로젝트의 산출물 중 하나다.** "AI가 왜 다르게 만들었는가"를 문서 품질의 문제로 환원해 기록하는 과정 자체가, 3번 역량(문서화)의 증거가 된다.

---

## 14. Open Questions

PRD를 완성하기 위해 확인이 필요한 항목이다.

| # | 질문 | 영향 범위 | 제안 |
|---|---|---|---|
| Q-1 | Product List는 **카테고리 목록만** 다루는가, **검색 결과**도 포함하는가? | FR-L 전반, E-16 | 카테고리 목록 중심 + 검색 결과는 동일 카드 재사용으로 처리 |
| Q-2 | 다크 모드를 지원하는가? | Color Foundation 설계량 | v1은 라이트 모드만. 단, 색 토큰은 테마 확장 가능한 구조로 설계 |
| Q-3 | 접근성 목표 수준은? | UR-S5, 색 대비 | WCAG 2.1 AA를 목표로 명시 |
| Q-4 | 데이터는 어떻게 준비하는가? (실제 상품 캡처 기반 샘플 / 완전 가상 데이터) | 구현 단계 전체 | 구조만 동일한 가상 데이터. 실제 브랜드·상품명 미사용 |
| Q-5 | 리뷰의 '핵심 정보' 항목을 무엇으로 확정할 것인가? (사용 기간 / 재구매 여부 / 선택 옵션 / 평점 외 추가?) | FR-D10, Review Card 설계 | 위 4개로 확정 |
| Q-6 | 강조 단계를 3단계로 정의하는 것이 적절한가, 2단계가 더 나은가? | UR-E1, 시스템 전체 | 3단계(Primary / Secondary / Ambient) 권장 |
| Q-7 | 구현 결과물을 어디까지 만드는가? (정적 화면 2개 / 상호작용 포함 프로토타입) | 구현 단계 범위 | 주요 상호작용(옵션 선택, 리뷰 접힘/펼침, 그룹 이동) 포함 |

---

## 15. 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.1 | 2026-08-31 | 최초 작성. 핵심 문제 3개 선정, Problem~Success Criteria 정의. Foundation/Component 미정의 |
| v0.2 | (예정) | Figma Foundation·Component 설계 결과 반영 |
